name: first Bash

on: 
  pull_request:
    branches: ["*"]

jobs:
    Bash:
      runs-on: ubuntu-latest
      steps:
       - uses: actions/checkout@v3
       - name: run bash command 
         env: 
          my_secret: ${{secret.my_secret }}
       - run: 
            echo $my_secret

            